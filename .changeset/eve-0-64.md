---
"@github-tools/eve-extension": patch
---

Rebuild the extension against eve 0.64. eve 0.64.0 dropped tool contract 53 and dynamic tool contract 51 with its sandbox session changes, so `eve build` rejected the 0.7.3 build on the newest eve. This release requires tool contract 54 and dynamic tool contract 52, which means agents must be on eve 0.64 or later.
